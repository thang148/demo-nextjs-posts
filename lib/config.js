const config = {
  BASE_API_URL: `${process.env.NEXT_PUBLIC_DOMAIN_API}/api/v1`,
  TIME_REQUEST_HOME_PAGE: 180 * 1000,
  TIME_REQUEST_POST: 180 * 1000,
  TIME_REQUEST_CATEGORY: 180 * 1000
}

export default config
